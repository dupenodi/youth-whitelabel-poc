name: youth_sdk
description: >
  YOUTH Health Tech white-label SDK.
  Drop into any Flutter app to embed AI-powered health screening
  with full brand customisation.
version: 1.0.0
homepage: https://youth-healthtech.com

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  provider: ^6.1.2
  cached_network_image: ^3.3.1
